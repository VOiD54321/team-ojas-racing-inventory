# Team Ojas Racing Inventory V12

Adds a working TABLE / GRID inventory switch. The selected mode is interactive and renders either the existing table or responsive part cards. Mobile uses one-column cards; desktop uses three columns. V11 mobile navigation and V10 local-network configuration are preserved.
